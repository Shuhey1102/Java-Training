# 課題仕様書 ③ コンソールアプリ：基本 CRUD + クラス設計

**フェーズ：** Phase 2-A　**期間目安：** 3 日　**担当：** 新人

---

## 目的

部品在庫管理アプリのコンソール版を段階的に作成する第一歩。
クラス設計・メニュー操作・データ管理を組み合わせて、動くアプリとして完成させる。

---

## 背景（業務イメージ）

あなたは工場の倉庫管理システムを開発することになりました。
倉庫の担当者が端末を操作し、部品の在庫を管理します。

まずは「メモリ上でデータを管理する」最小構成から始めます。
（ファイルや DB は次の課題で追加します）

---

## 作成するアプリの仕様

### プロジェクト構成

```
05_project_inventory_system/
└── src/
    └── inventory/
        ├── model/
        │   └── Part.java          ← 部品クラス
        ├── service/
        │   └── InventoryService.java  ← 在庫操作ロジック
        └── app/
            └── Main.java          ← エントリーポイント（メニュー操作）
```

---

### Part クラス（`model/Part.java`）

| フィールド | 型 | 説明 | 例 |
|---|---|---|---|
| `partCode` | `String` | 部品コード（ユニーク） | `P001` |
| `partName` | `String` | 部品名 | `ボルト M6` |
| `stock` | `int` | 在庫数（0 以上） | `100` |
| `warehouseCode` | `String` | 倉庫コード | `WH01` |

実装すること：
- 全フィールドを引数に持つコンストラクタ
- 各フィールドの getter / setter
- `toString()` メソッド

---

### InventoryService クラス（`service/InventoryService.java`）

内部で `List<Part>` を保持し、以下のメソッドを実装すること。

| メソッド | 説明 |
|---|---|
| `List<Part> findAll()` | 全部品を返す |
| `Part findByCode(String partCode)` | 部品コードで検索して返す（なければ `null`） |
| `void add(Part part)` | 部品を追加する。同じ部品コードが存在する場合は例外をスロー |
| `void updateStock(String partCode, int newStock)` | 在庫数を更新する。新在庫数が 0 未満の場合は例外をスロー |
| `boolean delete(String partCode)` | 部品コードで削除する。削除できた場合は `true`、見つからない場合は `false` を返す |

---

### Main クラス（`app/Main.java`）

起動するとメニューが表示され、番号を入力して操作できること。

**メニュー表示例：**
```
===== 部品在庫管理システム =====
1. 部品一覧表示
2. 部品登録
3. 在庫数更新
4. 部品削除
5. 部品検索
0. 終了
> 
```

**各機能の動作：**

#### 1. 部品一覧表示
```
===== 部品一覧 =====
部品コード  部品名          在庫数  倉庫
P001       ボルト M6       100    WH01
P002       ナット M6        50    WH01
P003       ワッシャー M6    200    WH02
```

#### 2. 部品登録
```
--- 部品登録 ---
部品コード: P004
部品名: スプリング
在庫数: 30
倉庫コード: WH02
登録しました。
```

エラー例（同一コードが存在する場合）：
```
エラー：部品コード P001 はすでに登録されています。
```

#### 3. 在庫数更新
```
--- 在庫数更新 ---
部品コード: P001
新しい在庫数: 150
更新しました。（P001：100 → 150）
```

エラー例（在庫数がマイナスの場合）：
```
エラー：在庫数は 0 以上で入力してください。
```

#### 4. 部品削除
```
--- 部品削除 ---
部品コード: P004
削除しました。
```

#### 5. 部品検索
```
--- 部品検索 ---
部品コード: P001
部品コード: P001 / 部品名: ボルト M6 / 在庫数: 150 / 倉庫: WH01
```

見つからない場合：
```
部品コード P999 は見つかりませんでした。
```

---

## 業務ルール

| ルール | 内容 |
|---|---|
| 部品コードの重複禁止 | 同じ部品コードは登録できない |
| 在庫数は 0 以上 | 登録・更新ともに 0 未満は不可 |
| 存在チェック | 更新・削除は存在しない部品コードを指定するとエラー |

---

## 初期データ

アプリ起動時に以下のデータが登録済みの状態にすること（`InventoryService` のコンストラクタで追加）。

| 部品コード | 部品名 | 在庫数 | 倉庫コード |
|---|---|---|---|
| P001 | ボルト M6 | 100 | WH01 |
| P002 | ナット M6 | 50 | WH01 |
| P003 | ワッシャー M6 | 200 | WH02 |

---

## 提出・確認方法

1. 全メニュー項目の動作デモをする
2. 正常系だけでなく、エラーケース（重複登録・マイナス在庫など）もデモすること
3. 担当者から「このクラスは何をしているか」を口頭で説明できること