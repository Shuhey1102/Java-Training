# 新人教育カリキュラム概要（Java・レガシーマイグレーションチーム向け）

## 概要

| 項目 | 内容 |
|------|------|
| 対象 | Java 学習経験あり（学習レベル）・バックグラウンド不明の新人 |
| 期間 | 約 1〜3 ヶ月 |
| テーマ | 部品在庫管理アプリ（生産管理系） |
| IDE | Eclipse（メイン）/ VS Code（補助） |
| DB | SQL Server |
| Web フレームワーク | Jakarta EE → Spring Boot（段階的移行） |
| 教育方針 | 基礎をしっかり固める・同一テーマで段階的に機能追加・自走力の育成 |

---

## フェーズ全体像

```
Phase 1               Phase 2-A                         Phase 2-B                    Phase 3
環境構築              コンソールアプリ                    Web アプリ                   プロジェクト参加
& Java 基礎           （部品在庫管理）                    （部品在庫管理）              & HOST 入門

① 環境セットアップ   ③ 基本 CRUD + クラス設計           ⑦ Jakarta EE Servlet+JSP     実業務タスク参加
② Java 基礎棚卸し    ④ 入出庫 + ファイル I/O            ⑧ CRUD Web アプリ完成        HOST 系概要インプット
                      ⑤ JDBC + SQL Server 連携           ⑨ Spring Boot へ移行         振り返り面談
                      ⑥ 設計整理・レビュー

[1〜2 週目]           [3〜5 週目]                         [6〜8 週目]                  [9〜12 週目]
```

---

## Phase 1：環境構築 & Java 基礎確認（1〜2 週目）

### ① 開発環境セットアップ（2〜3 日）

- JDK 17 LTS インストール・JAVA_HOME 設定
- Eclipse（Pleiades）インストール・日本語化
- VS Code + Extension Pack for Java インストール
- Git 初期設定（user.name / user.email）・Eclipse との連携
- Maven の動作確認（`mvn -version`）
- SQL Server への接続確認（A5:SQL Mk-2 等で `SELECT 1`）
- Eclipse で "Hello World" をビルド＆実行まで完走

> **ポイント：** Eclipse をメイン・VS Code を「調べもの用」と位置づけを最初に伝える。Pleiades の SSL/Proxy 設定は先回りして確認しておくと詰まりにくい。

### ② Java 基礎の棚卸し（3〜4 日）

- 型・演算子・制御構文（if / for / switch）を書いて動かす
- クラス・インスタンス・コンストラクタ・アクセス修飾子
- 継承・インターフェース・ポリモーフィズム
- List / Map / Set の基本操作
- 例外処理（try-catch-finally・独自例外クラス）
- Stream API・ラムダ式の基礎（filter / map / collect）

> **ポイント：** 学習経験ありの前提なので「説明してもらう」形で棚卸し。詰まるポイントがそのままレベル確認になる。部品在庫のドメイン用語（部品コード・在庫数・倉庫など）を例に使うと次フェーズへの橋渡しになる。

---

## Phase 2-A：コンソールアプリ「部品在庫管理」（3〜5 週目）

### アプリ概要

工場の倉庫係が端末で部品の入出庫・在庫を管理するシステムをコンソールアプリとして段階的に構築する。CSV → JDBC → DAO パターンの順で機能を追加することで、「なぜ DB が必要か」「なぜ設計パターンが必要か」を体感で学ぶ。

### ③ 基本 CRUD + クラス設計（3 日）

- `Part` クラス設計（部品コード・部品名・在庫数・倉庫コード）
- Scanner でメニュー選択（1:一覧 2:登録 3:在庫更新 4:終了）
- `List<Part>` にデータを保持し、一覧・登録・検索を実装
- 在庫数マイナス防止バリデーション（業務ルールを意識させる）

> **ポイント：** 在庫数・倉庫コードなど実務語彙を最初から使う。在庫数マイナス防止は後のトランザクション制御の伏線になる。

### ④ 入出庫トランザクション + ファイル I/O（3 日）

- `StockTransaction` クラス設計（入庫・出庫・日時・数量）
- 部品マスタを CSV から読み込む（BufferedReader）
- 入出庫履歴を CSV に書き出す（BufferedWriter・追記モード）
- ファイル不在時・フォーマット不正時の例外ハンドリング

> **ポイント：** 「マスタをファイルで管理する」→ 次ステップで DB に移行する伏線。入出庫ログを CSV に残すのは実際の生産管理でも頻出パターン。

### ⑤ SQL Server 接続（JDBC）+ DAO パターン（4 日）

- テーブル設計・DDL 作成（部品マスタ・入出庫履歴テーブル）
- mssql-jdbc ドライバ追加・接続文字列設定
- 部品マスタの SELECT / INSERT / UPDATE をコンソールから実行
- PreparedStatement で SQL インジェクション対策を体感
- 入出庫処理でトランザクション制御（在庫更新＋履歴 INSERT を同一 TX）
- DAO クラスに DB アクセスを分離（`PartDao` / `TransactionDao`）

> **ポイント：** CSV で管理していたものを DB に移行する流れにすると、DAO パターンの意味が体感できる。TX ロールバックは「在庫を引いたのに履歴が残らない」失敗例を先に見せると刺さる。

### ⑥ 設計整理（パッケージ・ログ・レビュー）（2 日）

- `model` / `dao` / `service` / `util` のパッケージ分割
- 業務例外クラスの定義（`StockException` など）
- SLF4J + Logback でログ出力追加
- Git に push → PR 作成 → コードレビュー → マージ

> **ポイント：** PR → レビュー → マージの一連フローを初体験させるタイミング。レビューコメントの書き方・受け取り方も教える。

---

## Phase 2-B：Web アプリ「部品在庫管理」 Jakarta EE → Spring Boot（6〜8 週目）

### アプリ概要

Phase 2-A で作ったコンソールアプリに「画面」を追加する形で Web アプリを構築する。まず Jakarta EE（Servlet + JSP）で実装し、その後 Spring Boot で同じものを再実装することで、「フレームワークが何を肩代わりしているか」を比較学習する。

### ⑦ Jakarta EE：Servlet + JSP で画面化（4〜5 日）

- Tomcat セットアップ・Eclipse との連携
- Servlet で GET/POST を受けてリクエスト処理
- JSP で部品一覧画面を表示（EL 式・JSTL）
- セッション・リダイレクト・フォワードの違いを体験
- コンソールアプリの DAO をそのまま流用して一覧を表示

> **ポイント：** 「DAO はそのまま使える」を体感させることが最大の目的。Jakarta EE をやる理由（レガシー資産の構造理解）を最初に伝える。

### ⑧ Jakarta EE：CRUD Web アプリ完成（3〜4 日）

- 部品登録・在庫更新・入出庫登録の画面を実装
- バリデーション（必須・数値・在庫数マイナス）を手動実装
- 入出庫履歴の一覧画面（日付・部品コードで絞り込み）
- エラー画面（404 / 500 カスタムエラーページ）

> **ポイント：** 「コンソールで作ったものが画面で動く」達成感がモチベーション維持の鍵。

### ⑨ Spring Boot へ移行・比較学習（5〜6 日）

- Spring Initializr でプロジェクト生成（Web / Thymeleaf / JDBC）
- `@Controller` / `@Service` / `@Repository` アノテーション理解
- 同じ CRUD を Spring Boot で再実装（Jakarta EE との差分を比較）
- `application.properties` で SQL Server 接続設定
- JdbcTemplate で DAO を書き換え（接続管理が消える体験）
- 「Jakarta EE と何が変わったか」を口頭で説明させる

> **ポイント：** 同じアプリを 2 回作ることで「フレームワークが何を肩代わりしているか」が体感できる。これが Phase 3 のレガシー理解にも直結する。

---

## Phase 3：プロジェクト参加 & HOST 入門（9〜12 週目）

| 内容 | 詳細 |
|------|------|
| 実業務タスク参加 | マイグレーション業務の小スコープタスクをアサイン |
| テスト担当 | テスト仕様書の作成・テスト実施を担当 |
| HOST 系概要インプット | COBOL/JCL の概要（移行元を知る目的） |
| HOST 担当との交流 | チームの HOST 担当メンバーと週 1 程度の 1on1 ペアリング |
| 振り返り | 3 ヶ月の振り返りシートで自己評価 → フィードバック面談 |

> **ポイント：** HOST 入門は Java にある程度自信がついてから実施。「こっちの世界はこう書くのか」という比較軸ができた状態で入ると吸収が早い。

---

## 教育担当メモ

| 観点 | 内容 |
|------|------|
| レベル確認 | Phase 1 で自走できるかを見極め、遅ければ Phase 2 の開始を 1 週ずらす |
| HOST 入門のタイミング | Phase 3 後半に回す（Java に自信がつく前に触れると混乱しやすい） |
| 定期確認 | 週次で「詰まっていること」を聞く 15 分の確認を設けると失踪リスクが下がる |
| レビュー | Phase 2-A ⑥ のタイミングで初 PR レビューを実施。コメントの書き方・受け取り方も教える |
| テーマの一貫性 | 全フェーズを通じて「部品在庫管理」で統一。題材が変わると「何を学ぶか」より「何を作るか」に頭が向きがちになる |
